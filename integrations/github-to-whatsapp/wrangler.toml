name = "github-to-whatsapp"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets (configurar con wrangler secret put):
# GHL_INBOUND_WEBHOOK_URL → URL del Inbound Webhook en tu workflow de GHL
