# Este workflow está desactivado — reemplazado por gemini-agent.yml
# Copilot coding agent no puede ser disparado programáticamente (limitación de GitHub).
# El agente Gemini (gemini-agent.yml) realiza la misma función de forma 100% automática.

name: Auto-assign Copilot to AI Tasks (DISABLED)

on:
  workflow_dispatch: # Solo manual — no se dispara automáticamente
